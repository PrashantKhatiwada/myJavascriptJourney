//Variables and Datatypes

var fullName = "Prashant Khatiwada"; //String

var courseName = "Javascript Course with Hitesh"; //string

var isLoggedIn = true; //Booleans

var loggedCount = 34; //Integers

console.log(loggedCount); //Prints 34 as the value assigned to the variable loggedCount is 34

console.log("loggedCount"); //Prints the string "loggedCount" as we put it inside the quotes

var paymentMode;

console.log(paymentMode); //Undefined

paymentMode = "Credit Card"; //string

console.log(paymentMode); //prints the string Credit Card